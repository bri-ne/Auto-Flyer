name: authorize
on:
  push:
    branches:
      - main

jobs:
  my-job:
    runs-on: ubuntu-latest
    env:
      MY_ENV_VAR: ${{ secrets.GCAL_KEY }}
    steps:
      - name: Vars
        run: echo $MY_ENV_VAR
